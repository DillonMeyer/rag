[project]
name = "rag"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "fastapi",
  "uvicorn",
  "sqlmodel",
  "psycopg[binary]",
  "pgvector",
  "pymupdf"
]

[tool.uv]
dev-dependencies = [
  "ruff>=0.3",
]